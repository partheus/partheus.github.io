---
layout: page
title: "Weeknotes"
permalink: /weeknotes/
tags: "footer"
eleventyNavigation:
  key: weeknotes
  order: 2
description: "What goes on..."
---
<ul class="display-columns display-columns--two article__list">
    {% for article in collections.weeknotes | reverse %}
        <li class="article__item">
            <article class="article__preview">
                <h3 class="article__preview__header"><a href="{{ article.url | url }}">{{ article.data.title }}</a></h3>
                <p class="article__preview__description">{% if article.data.description %} {{ article.data.description }} &#x10101; {% endif %}
                    <small>
                        <time>{{ article.data.date | readableDate }}</time>
                        {% if article.data.tags %}
                            {% for tag in article.data.tags.slice(1, 3) %}
                                {% if tag != 'weeknotes' %}{% set tagUrl %}/tags/{{ tag | slug }}/{% endset %}
                                    <a class="category"
                                       href={{ tagUrl | url }}>{{ tag }}
                                    </a>
                                {% endif %}
                            {% endfor %}
                        {% endif %}
                    </small>
                </p>
            </article>
        </li>
    {% endfor %}
</ul>
