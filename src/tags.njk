---
layout: "base.njk"
pagination:
  data: collections
  size: 1
  alias: tag
  filter:
    - all
permalink: /tags/{{ tag }}/
title: Tagged content
---
<div class="irevamp-hero">
<h3 style="margin-bottom: 0.6em;" class="bold600">Here are all entries tagged “{{ tag }}”:</h3>
</div>
<main class="irevamp-container">
<div class="irevamp-section slide-up">
    <ul class="irevamp-list-unstyled irevamp-postlist">
        {% set taglist = collections[tag] | reverse %}
        {% for post in taglist %}
            <li class="irevamp-postlist__content">
                <a href="{{ post.url }}" class="irevamp-text">{{ post.data.title }}</a>
                <p style="margin-top: 5px" class="irevamp-lead">{{ post.data.date | postDate  }}
                    {% if post.data.description %} • {{ post.data.description }}
                    {% endif %}
                </p>
            </li>
        {% endfor %}
    </ul>

    <div class="irevamp-h5 irevamp-mt-md bold600">Go back to <a href="/entries">all entries</a>.  Or visit <a href="/writing">the writing section</a> again.</div>
</div>
</main>