{% from '../macros/projectCard.njk' import projectCard %}

<div class="display-columns display-columns--two mt-sm">
    {% for card in featuredArchiveProjectCards %}
        {{ projectCard(
            title=card.title,
            projectUrl=card.projectUrl,
            imageUrl=card.imageUrl
        ) }}{% endfor %}
</div>
